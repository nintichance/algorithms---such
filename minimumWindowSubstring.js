//Given a string S and a string T, find the minimum window in S, which will contain all the characters in T in complexity O(n)
//76
const minWindow = (s, t) => {
  //if all three letters are shown, i can increment
  //j will increment always
  //need to keep up with min string
  //min string hash array
  let i = 0
  seen = {}
  for(let j=0; j<s.length; j++){
    
  }
}